{% macro game_renderer(game_data) %}
  <article class="shadow-lg bg-white text-black">
    <a href="{{ game_data.link }}" class="block">
        <div class="overflow-hidden h-48 relative">
            <img alt="Placeholder" class="absolute block h-auto h-full object-cover top-0 w-f w-full" src="/static/img/bg-1.jpg">
        </div>
        <header class="p-2 px-5 bg-white relative">
            <h2 class="text-2xl font-heading mt-2">{{ game_data.name }}</h2>
            <p><?php echo $randomDate = date("d M Y", $timestamp); ?> at 7:30pm <span class="text-xs">(GMT)</span></p>
     </header>
     <div class="bg-dark-blue grid grid-cols-4 text-xs text-white px-5 py-3">
        <div class="flex items-center">
            <img src="/static/img/user.svg" class="w-5 mr-2">
            <span class="pt-1">{{ game_data.free_spaces }}</span>
        </div>
        <div class="flex items-center">
            <img src="/static/img/dice.svg" class="w-5 mr-2">
            <span class="pt-1">3</span>
        </div>
        <div class="flex items-center">
            <img src="/static/img/dice.svg" class="w-5 mr-2">
            <span class="pt-1">3</span>
        </div>
        <div class="flex items-center">
            <img src="/static/img/calendar.svg" class="w-5 mr-3">
            <span class="pt-1">20/12/06</span>
        </div>
    </div>
</a>
</article>
{% endmacro %}

{% for game_data in games_data %}
  {{ game_renderer(game_data) }}
{% endfor %}
