{% extends 'base.jinja' %}

{% block content %}
  <h1>{{ game.name }}</h1>
  <p>{{ game.description }}</p>
  <ul>
    {% for player in game.players.all() %}
      <li>{{ player }}</li>
    {% endfor %}
  </ul>
{% endblock %}
